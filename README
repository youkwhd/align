align
=====

Text alignment utility.

Quick Start
-----------

This assumes you already have sbcl installed:

    ./build.sh

Examples
--------

Examples can be found within the examples/ folder. The input is
identified by *.in files, expected output will be *.out files.

The *.cmd files represents the commands used to produce output.
